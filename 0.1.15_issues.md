- there are time when a drop down doesnt display in the correct place? it seems anchored to an area that isnt where you expect? 
- drop down menus also seem to be layered strange on the dashbaord pages, the element layers over the top of the dropdown? 
- can we save open and collapse status of the report/dashboard/model groups? 
- model canvas pages dont seem to save and restore your viewport position on .flograph file reopen? 
- conditional formatting we should be able to set wildcards etc in icon values and anywhere value rules are set. 
- dragging around pages to reorder them casuses the model to expand and makes the dragging annoying
- maybe we add a second row to the tabs, so when model is clicked, we see models in the lower row, and if group x is clicked we see the contained items in the row below, groups and items can be on the first initial row, and items can be on the initial row or secondary row? 
- when i have two flograph apps open with different files only one app can show plotly graphs? 
- Add a spellcheck uk/us (uk by default) onto the report page and report and note nodes. only view spell check errors and suggestions when not locked or in edit mode. not on final outputs. 
- slicer needs an update where we can have trees of different lengths, lets say a file thats 4 deep but then a file thats 2 deep. @