1. when i exit a node after typing some code and then nav back it doesnt save my temp changes and resets? 
2. flaky test-suite crash at process exit (~25% of runs, pre-existing on master): `double free or corruption` / `QBasicTimer::stop ... different thread` abort during garbage collection. Repro: run `tests/test_show_plot_node.py tests/test_show_table_node.py tests/test_view_keyboard.py` together a few times (offscreen). Involves jedi completion threads (spawned via the editor dock in MainWindow tests) still running while Python GC tears down Qt object cycles; tests themselves all pass — the crash is in teardown. Likely fix directions: shut down jedi/completion threads in MainWindow close, or break Python reference cycles through QWidgets so their teardown doesn't depend on the GC.
3. Not sure if this is an issue but when running on bazzite, python, in pycharm i get this error: GBM is not supported with the current configuration. Fallback to Vulkan rendering in Chromium.
4. flow "buttons" nodes are flakey, snapping is strange, only when you drag neaar the top of the button, and also resizing the button is not avaliable. and when you click a node and then click the button to move it around it selects the button and the node and moves them both, might be worth only making buttons "editable" after right clicking first, then its in "edit mode" where yoou can resize and move around.
5. when selecting more than one frame and node on the canvas and you drag to move, only the node the cursor is over is snapped the rest seem to move with no snapping.
6. the custom "card" visual i saved, when i drag it on its not the card but a normal node? 