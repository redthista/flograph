"""The bundled word lists the spell check reads.

Data only — the checker itself is `flograph.core.spelling`, which is
Qt-free and finds these through `importlib.resources`. See SOURCES.md for
where the words came from and `scripts/build_word_lists.py` for how they
are made.
"""
