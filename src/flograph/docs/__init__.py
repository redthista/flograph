"""The bundled handbook — Markdown pages shown by Help ▸ Documentation.

These `.md` files are packaged data, read at runtime via
`flograph.core.docpages`. They are written GitHub-wiki compatible
(`[[Page Title]]` links, one `# H1` per file, `Title-Case-Dashes.md`
names) so the same files can be published to a hosted wiki unchanged.
Edit a page in the same commit as the feature it documents.
"""
