- allow addition of canvas "notes" to the dashboard pages. and report pages
- ability to hide index on show table.
- add a open in new tab option to right click of a frame, where it shows a new canvas tab with only that frame.
- a from node that takes a .flograph file and is able to extract either from cache or run the model to get the table in question. 
- install packages for pip, needs to allow for a configured pip repository
- slicer drop downs, the drop down seems to be at the same layer as the slicer, so when i click it can be hidden by item infront if its z level? can we fix so sliver drop downs are always ontop? 
- Easy way to see pip and weblibrary requirements of a flograph file?
- plain canvas by default
- scroll bars on by default
- allow action buttons to take you to a page
- A way to group tabs together
- when you hover over any table header you should see the full name, this is nice for columns where the size has been set and the col expands. this is on canvas and dashboard pages. 
- dashboard navigation buttons? navigation bar? 
- the edited 6mins ago could go inline with the workflow names and allow the location to then span the width? 
- right click edit properties on canvas nodes
- if i right click the <> on the tabs bar when theres lots of tabs id like to see the tab list i can pick from. this makes it easier to navigate flos where there are lots of tabs. 
- ability to "pivot" in the show tables, and conditional formatting could alsoo apply prepivot, this could simplify conditional formatting where theres like 6 columns and then 6 checker columns that feed the icons shown.